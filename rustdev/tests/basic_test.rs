//! Basic integration test to verify the library works

#[test]
fn test_library_compiles() {
    // This test just verifies the library compiles
    assert!(true);
}

#[test]
fn test_basic_math() {
    assert_eq!(2 + 2, 4);
    assert_eq!(10 * 10, 100);
}